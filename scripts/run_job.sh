#!/bin/bash

# TODO: Add commands to build the jar, upload it (if needed), and run the MapReduce job.
# Example steps:
# mvn clean package
# hdfs dfs -rm -r /user/hduser/digital-librarian/output || true
# hadoop jar target/digital-librarian.jar digital.librarian.ReverseIndexDriver \
#     /user/hduser/digital-librarian/input /user/hduser/digital-librarian/output

# NOTE: Add logging and argument parsing for input/output paths.
